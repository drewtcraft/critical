<% if current_user %>
  <span>
    hey <%= current_user.name %>
  </span>
  (<%= button_to(
   "Log Out",
   destroy_user_session_path,
   method: :delete
  ) %>)
<% else %>
  <%= link_to "sign_in", new_user_session_path %>
<% end %>
